# Панель управления

Вход: логин и пароль. Значения хранятся в `admin/auth.json` (соль + sha256-хеш).

Смена логина или пароля — через коммит этого файла, настройки Vercel не нужны.
Хеш: `sha256(salt + ':' + password)`.

Забытый пароль восстанавливается одним коммитом с новой солью и хешем.
