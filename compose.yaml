services:
  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"
    volumes:
      - ./runtime/data:/app/data
      - ./runtime/uploads:/app/site/uploads
    security_opt:
      - no-new-privileges:true
